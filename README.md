# Sudoku Solver Api

The core algorythm is a smart brute force applied recursively.

## Getting Started

```shell
  $ make install
  $ make local
  $ make test
  $ make cover
```

### Test client

```shell
  $ curl localhost:8080/sudoku
```