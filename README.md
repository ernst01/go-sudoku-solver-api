# Sudoku Solver Api

The core algorythm is a simple brute force applied recursively.

## Getting Started

```shell
  $ make install
  $ make run
  $ make test
  $ make cover
```

## Test client

```shell
  $ curl localhost:8080/sudoku
```